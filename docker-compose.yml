services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: candidate_intelligence
      POSTGRES_USER: candidate_app
      POSTGRES_PASSWORD: candidate_app
    ports:
      - "5432:5432"
    volumes:
      - candidate-postgres:/var/lib/postgresql/data

volumes:
  candidate-postgres: